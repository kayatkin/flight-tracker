## [1.11.2] — 2026-09-17

### Изменено

- История на широком экране (≥720px): две колонки маршрутов. В Telegram и на телефоне по-прежнему одна. Карточки те же.
- Форма «Добавить» на десктопе не растягивается: колонка до 640px, «Откуда / Куда» и «Пассажиры / Цена» стоят рядом. На телефоне поля столбиком как раньше.

## [1.11.1] — 2026-09-15

### Изменено

- Панель истории: сортировка по ширине текста, счётчик «N билетов» / «N из M», кнопка экспорта `⬇️ CSV`. Всплывающие подсказки те же.

## [1.11.0] — 2026-09-15

### Добавлено

- Цены остаются в рублях, рядом показывается долларовый эквивалент по курсу ЦБ РФ (`cbr.ru`) **на дату внесения** билета (`dateFound`)
- Edge Function `fx-usd` ходит на официальный XML ЦБ (браузер сам `cbr.ru` не вызывает: нет CORS)
- В CSV появились колонки «Цена всего USD», «На человека USD» и курс ЦБ

### Исправлено

- CSV снова открывается таблицей в русском Excel: разделитель `;`, первая строка `sep=;`, UTF-8 BOM, перевод строк CRLF, даты `ДД.ММ.ГГГГ`

## [1.10.0] — 2026-09-15

### Исправлено

- Второй Telegram на edit-ссылке больше не получает правку после refresh: права = пересечение приглашения и выданного refresh
- Выход из гостевого отзывает opaque refresh, а не только перезагружает вкладку
- Bind edit-ссылки атомарный: первый Telegram выигрывает гонку
- `auth-guest` не возвращает plaintext share-токен в теле ответа
- Форма нового пароля не открывается по голому `?type=recovery` без OTP

### Добавлено

- Best-effort лимит частоты на публичные Edge Functions (429 + Retry-After)
- Один in-flight `auth-refresh` на клиенте, чтобы две вкладки не отзывали family
- CSP: `object-src 'none'`, `frame-ancestors` для Telegram Web, localhost connect только в dev

### Изменено

- Новый пароль (регистрация, сброс, связка email) — не короче 8 символов; вход со старым коротким паролем ещё работает

## [1.9.0] — 2026-09-14

### Добавлено

- Dual-key JWT в Edge Functions: при `JWT_SIGNING_PRIVATE_JWK` access подписывается ES256 с `kid`, иначе по-прежнему HS256
- `verifyOwnerToken` проверяет ES256/RS256 по JWKS и HS256 по `JWT_SECRET` (гости по-прежнему отклоняются)
- Deno-тесты roundtrip HS256/ES256/RS256 в CI

### Изменено

- `link-email` деплоится с `--no-verify-jwt`: после ротации signing keys шлюз Auth ломает gateway-verify; проверка владельца остаётся в функции

## [1.8.0] — 2026-09-14

### Добавлено

- Настоящий refresh для Telegram/гостя/dev: opaque-токен в `refresh_tokens`, ротация через `auth-refresh`, access живёт 1 час
- Гостевая сессия переживает обновление вкладки, пока share-ссылка жива (не дольше суток)
- Каталог русских строк `src/shared/i18n`: экран входа, шапка, форма, история, шаринг и ошибки вместо литералов в JSX

### Изменено

- Custom JWT помечается `ft=custom`, чтобы не путать с email GoTrue после access-token hook
- Legacy-сессии, где refresh = access, работают до истечения

## [1.7.0] — 2026-09-14

### Добавлено

- Связка Telegram и email в один аккаунт: таблица `user_identities`, кнопка **Аккаунт**, Edge Function `link-email`
- Привязка email из Mini App требует пароль; если ящик уже есть — проверяем его. Истории сливаются в сторону той, где больше билетов (при равенстве остаётся текущая сессия)
- Миграция `008_user_identities.sql`: канонический `user_id` в Custom Access Token hook и в `auth-telegram`

### Изменено

- Email-вход после связки читает `user_id` из JWT хука, а не GoTrue UUID, чтобы открывалась общая история

## [1.6.1] — 2026-09-14

### Исправлено

- Ссылка «забыли пароль» открывает экран нового пароля: `token_hash`, hash-токены и флаг в sessionStorage, а не только событие `PASSWORD_RECOVERY` после загрузки приложения

### Изменено

- На сбросе пароль нужно повторить; после письма просим открыть ссылку в том же браузере

## [1.6.0] — 2026-09-13

### Добавлено

- Вход, регистрация и сброс пароля по email в браузере (тот же SPA, без форка)
- Кнопка «Выйти» у email-владельца; в DEV — «Войти как разработчик» на экране входа
- Миграция `007_email_owner_auth.sql`: `is_owner()` принимает GoTrue JWT без `app_role`, Custom Access Token hook помечает email-сессию как owner

### Изменено

- Браузер без Telegram больше не открывает приложение как «Только на устройстве» — сначала экран входа
- GoTrue session для email включает auto-refresh и PKCE; custom JWT Telegram/гостя по-прежнему не обновляются

## [1.5.2] — 2026-09-13

### Добавлено

- На карточке маршрута **⬇️** скачивает только это направление

### Изменено

- Заметка растягивается только вниз и не выше ~6 строк, дальше прокрутка внутри поля
- Поиск, сортировка, счётчик и CSV истории умещаются в две строки

## [1.5.1] — 2026-09-13

### Добавлено

- Coverage Vitest в CI (`npm run test:coverage`): пол ≥ 20% lines/statements, ≥ 60% functions/branches
- ESLint для `bot/**/*.js` вместе с корневым `npm run lint`
- `deno lint` для Edge Functions рядом с `deno check` (URL-импорты `esm.sh` как у Supabase; убран лишний `async` у HMAC)

### Изменено

- Vitest 3.2.7 (патч закрывает critical path-traversal UI, без перехода на Vitest 5)

### Исправлено

- Убраны неиспользуемые CSS-классы в форме, истории, шаринге и графике
- `lookup` бота больше не оставляет неиспользуемый `catch (error)`

## [1.5.0] — 2026-09-11

### Добавлено

- Экран восстановления, если интерфейс упал (Error Boundary)
- Черновик правки билета переживает обновление вкладки, отдельно от черновика новой формы
- Подтверждение закрытия вкладки браузера, пока форма грязная
- Клавиатура на аккордеоне доступа (Enter / Space, как у групп истории)

### Изменено

- В облако уходят только изменённые билеты и явные удаления, а не полный снимок
- Новые приглашения хранят хеш токена; ссылку копируют сразу после создания
- Edit-ссылка привязывается к первому Telegram-пользователю, который её открыл
- CORS Edge Functions ограничен GitHub Pages и localhost
- Деплой на Pages ждёт lint, тесты, бот, Deno functions и RLS
- Username бота больше не зашит в клиент: без `VITE_TELEGRAM_BOT_USERNAME` edit-ссылка становится обычной web-ссылкой

### Исправлено

- Favicon на GitHub Pages (`./favicon.svg` вместо `/favicon.ico`)
- Удалены мёртвые файлы и дубль модалки «Поделиться» в шапке

## [1.4.0] — 2026-09-10

### Добавлено

- Черновик новой формы переживает обновление вкладки (Cmd+R / F5), пока не сохранили билет
- Сортировка истории: по маршруту, сначала дешёвые, сначала новые или старые
- Необязательная заметка на билете (до 500 символов), поиск и CSV её видят
- CI проверяет RLS на временной Postgres

### Безопасность

- `npm audit --audit-level=high` в CI фронта; high/critical закрыты. Vitest 3 ещё moderate (без перехода на Vitest 5)

## [1.3.0] — 2026-09-10

### Добавлено

- После удаления билета тост «Билет удалён» с кнопкой **Вернуть** (тот же UUID)

## [1.2.1] — 2026-09-10

### Добавлено

- Поиск в истории запоминается при переключении вкладок

### Исправлено

- Бот больше не тянет `@supabase/supabase-js` (падение на Node 20 из‑за WebSocket)
- Если `api.telegram.org` недоступен, бот пишет, что Mini App запускается через `npm run dev`, и не крутит fatal-рестарт

## [1.2.0] — 2026-09-10

### Добавлено

- Предупреждение при уходе с несохранённой формы (вкладка История и «Отмена»)
- Focus trap в модалках: Tab крутится внутри окна, фон `inert`, Escape и возврат фокуса
- RPC `lookup_share_invite` — бот проверяет приглашение anon-ключом, без service role

### Исправлено

- Окно списка приглашений закрывается по Escape, как остальные модалки
- Сообщение бота больше не показывает кусок share-токена

### Безопасность

- Бот не читает БД с service role; `node-telegram-bot-api` обновлён до 0.67
- CI проверяет Edge Functions (`deno check`) и тесты бота

## [1.1.1] — 2026-09-10

### Добавлено

- Сброс отложенного сохранения при скрытии вкладки / закрытии Mini App
- Подтверждение закрытия Telegram, пока билет ещё не ушёл в облако
- Индикатор сохранения в шапке: «Сохранение…» / «Сохранено» / «Не сохранено» + Повторить
- Закрытие окон шаринга и присоединения по Escape
- Живое обновление темы Telegram и системной dark/light

### Исправлено

- Копирование ссылки в WebView, где `navigator.clipboard` недоступен

### Безопасность

- GitHub Pages больше не деплоится, если lint / typecheck / test красные
- Удалена неиспользуемая зависимость `@telegram-apps/sdk`

## [1.1.0] — 2026-09-08

### Добавлено

- Редактирование сохранённого билета с тем же UUID
- Дублирование билета (новый UUID, сегодняшняя дата поиска)
- Экспорт видимой истории в CSV (UTF-8 с BOM)
- Каталоги подсказок городов и авиакомпаний (без жёсткой валидации)
- Баннер режима редактирования и закрытие графика по Escape

### Изменено

- Группа в истории раскрывается по заголовку, а не по всей карточке
- Поиск учитывает регистр по `ru-RU`; счётчик показывает «найдено из всех»
- Склонение «пассажир / пассажира / пассажиров»
- Кнопки на карточке с зоной нажатия 44×44

### Безопасность

- См. отдельный [docs/SECURITY.md](./docs/SECURITY.md): что можно коммитить, как ротировать ключи, какие миграции обязательны

## [1.0.0] — 2026-05-10

### Добавлено

- ✈️ Форма добавления рейсов: oneWay / roundTrip, пересадки, прилёт на следующий день
- 📊 Анализ цен: сравнение с лучшим ранее сохранённым (±500 ₽ порог)
- 📈 Сезонный график цен (Chart.js) — минимальная цена по месяцам
- 📱 История рейсов в стиле iOS Wallet с группировкой и поиском
- 👥 Система совместного доступа (sharing):
  - Создание одноразовых ссылок с правами view/edit
  - Присоединение по токену через Telegram бота
  - Список активных сессий доступа
- 🔐 Гостевой режим с хранением данных в localStorage
- 🤖 Telegram бот: /start, /start share\_\<token\>, /help
- 🎨 Адаптивная тёмная/светлая тема через Telegram Theme API
- 🧪 Unit-тесты: validation (11), flightAnalysis (10), seasonalChart (12)
- 🚀 CI/CD: автоматический деплой на GitHub Pages через Actions
- 📦 Feature-based архитектура
- 🎯 Design tokens (CSS-переменные)

### Исправлено

- Исправлена обработка Telegram ссылок и гостевого режима
- Исправлен camelCase/snake\_case в Telegram API
- Исправлено дублирование информации о госте в интерфейсе
- Исправлена ошибка `closeModal is not defined`
- Убран неиспользуемый импорт GuestIndicator из HistoryView
- Исправлено копирование ссылки в SharedSessionsList
- Исправлена обработка share token

### Изменено

- Упрощён дизайн секции стоимости
- Убран дубликат заголовка в секции авиакомпании
- Убрана избыточная подсказка в секции авиакомпании
- Убраны избыточные иконки пассажиров
- Улучшен UX ссылок для совместного доступа
- Компактный дизайн карточек приглашений
- Улучшена адаптация карточек для мобильных устройств
- Улучшен заголовок ShareFlightModal

---

## Типы изменений

| Тип | Описание |
|-----|----------|
| **Добавлено** | Новая функциональность |
| **Исправлено** | Исправления багов |
| **Изменено** | Изменения существующей функциональности |
| **Удалено** | Удалённая функциональность |
| **Безопасность** | Исправления уязвимостей |